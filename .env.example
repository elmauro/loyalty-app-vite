# API base URLs (sin barra final).
# En desarrollo: NO definas VITE_API_BASE_AUTH ni VITE_API_BASE_APP para usar el proxy de Vite (evita CORS).
# En producción o para llamar directo al backend, define las URLs completas.
#
# Auth y OTP (login, envío OTP)
# VITE_API_BASE_AUTH=https://loyaleasy.com
# Transacciones, historial, saldo de puntos, tenant-api, admin-api (programa)
VITE_API_BASE_APP=https://dev.loyaleasy.com
# Program ID (header x-program-id)
VITE_PROGRAM_ID=PCM
# API Key (header x-api-key)
VITE_API_KEY=XltDmAkEaf73Pa63gQuPD9S8WCr83Ry73LF7g9wz
# Transaction type por contexto (header x-transaction-type)
VITE_TRANSACTION_TYPE_INCOME=sale
VITE_TRANSACTION_TYPE_EXPENSE=redemption

# Base paths Opción C (tenant-api, admin-api). Opcionales; por defecto: tenantsGet53rv1c3, tenantsPost53rv1c3, etc.
# VITE_TENANTS_GET_PATH=tenantsGet53rv1c3
# VITE_TENANTS_POST_PATH=tenantsPost53rv1c3
# VITE_TENANTS_PUT_PATH=tenantsPut53rv1c3
# VITE_ADMIN_PROGRAM_PATH=adminProgram53rv1c3

# Código de país para teléfonos (ej. 57 Colombia). Se antepone al número en acumulación, OTP y redención.
VITE_PHONE_COUNTRY_CODE=57

# Cognito (registro, login, recuperación y cambio de contraseña). Si no se definen, se usa solo login tradicional.
# VITE_COGNITO_USER_POOL_ID=us-east-1_xxxxx
# VITE_COGNITO_CLIENT_ID=xxxxxxxxxxxxxxxxxxxxxxxxxx
# VITE_COGNITO_REGION=us-east-1

# Solo para pruebas e2e (Cypress). No definir en .env para uso normal; cy:e2e lo inyecta.
# VITE_USE_MSW=true
#
# E2E contra backend real: no uses VITE_USE_MSW. Usa cy:e2e:real o cy:e2e:run:real
#
# LocalStack (backend en LocalStack): npm run dev:localstack
# Requiere: LocalStack + APIs desplegadas, node postman/update-postman-api-ids.js (en loyalty-program-serverless)
# El script inicia un proxy que enruta a las APIs; usa .env.localstack como referencia
# y define arriba VITE_API_BASE_AUTH, VITE_API_BASE_APP y VITE_API_KEY.