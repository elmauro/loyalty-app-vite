/** Tamaño de chunk por request al backend (paginación híbrida). El frontend muestra 10, 20 o 50 por página. */
export const BACKEND_CHUNK_SIZE = 100;

/** Grids de programa (Aliados, Oficinas, Administradores): primera página por defecto. Historial de transacciones usa 20. */
export const PROGRAM_GRID_DEFAULT_PAGE_SIZE = 10;
